import type { MetadataRoute } from "next";import { pages,siteUrl } from "./content";export const dynamic="force-static";export default function sitemap():MetadataRoute.Sitemap{return[{url:siteUrl,lastModified:new Date(),changeFrequency:"weekly",priority:1},...pages.map(p=>({url:`${siteUrl}/${p.slug}/`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:p.slug==="builds"?.9:.7}))]}
